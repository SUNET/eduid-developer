global
    log stdout  format raw  local0  debug

    daemon
    maxconn 256
    # Expose control socket to the haproxy_control volume, where other
    # containers such as the haproxy-status sidekick can access it.
    stats socket /var/run/haproxy-control/stats mode 600

    #user x
    group www-data

    # Default SSL material locations
    ca-base /etc/ssl/certs
    crt-base /etc/ssl/private

    ssl-default-bind-ciphers ECDH+AESGCM:DH+AESGCM:ECDH+AES256:DH+AES256:ECDH+AES128:DH+AES:ECDH+3DES:DH+3DES:RSA+AESGCM:RSA+AES:RSA+3DES:!aNULL:!MD5:!DSS
    ssl-default-bind-options no-sslv3
    tune.ssl.default-dh-param 2048

    max-spread-checks 10s
    spread-checks 5

defaults
    log global
    mode http
    option httplog
    option dontlognull
    timeout check 5s
    timeout connect 4s
    timeout client 17s
    timeout server 17s
    timeout http-request 5s
    balance roundrobin

    # never fail on address resolution
    default-server init-addr libc,none

resolvers docker_dns
    parse-resolv-conf
    accepted_payload_size 8192

# Redirect _everything_ to HTTPS
frontend http-frontend
    bind 0.0.0.0:80
    bind :::80

    redirect scheme https code 301

frontend dashboard-http
    bind *:443  ssl  crt /etc/ssl/dashboard.pem  alpn h2,http/1.1
    stats enable
    timeout http-request 10s
    timeout http-keep-alive 4s

    option forwardfor
    http-request set-header X-Forwarded-Proto https
    http-request set-header X-Forwarded-Host %[req.hdr(Host)]

    # The information is intended for a single user and must not
    # be cached by a shared cache and should always be revalidated.
    http-response set-header Cache-Control "no-cache, no-store, must-revalidate"
    http-response set-header Pragma "no-cache"
    http-response set-header Expires "0"

    acl should_redirect path               / OR ""
    http-request redirect location /profile/ if should_redirect
    use_backend favicon if { path /favicon.ico }

    use_backend authn if { path_beg /services/authn/ }
    use_backend security if { path_beg /services/security/ }
    use_backend ladok if { path_beg /services/ladok/ }
    use_backend letter-proofing if { path_beg /services/letter-proofing/ }
    use_backend orcid if { path_beg /services/orcid/ }
    use_backend jsconfig if { path_beg /services/jsconfig/ }
    use_backend pdata if { path_beg /services/personal-data/ }
    use_backend email if { path_beg /services/email/ }
    use_backend freja-eid if { path_beg /services/freja-eid/ }

    default_backend redirect_www

backend redirect_www
    http-request redirect location https://html.eduid.docker%[path] code 301

backend ladok
    option httpchk GET /services/ladok/status/healthy
    http-check expect string STATUS_OK_
    server ladok1 eduid-ladok-1:8080 check resolvers docker_dns  init-addr none

backend letter-proofing
    option httpchk GET /services/letter-proofing/status/healthy
    http-check expect string STATUS_OK_
    server letterproofing1 eduid-letter_proofing-1:8080 check resolvers docker_dns  init-addr none

backend orcid
    option httpchk GET /services/orcid/status/healthy
    http-check expect string STATUS_OK_
    server orcid1 eduid-orcid-1:8080 check resolvers docker_dns  init-addr none

backend jsconfig
    option httpchk GET /services/jsconfig/status/healthy
    http-check expect string STATUS_OK_
    server jsconfig eduid-jsconfig-1:8080 check resolvers docker_dns  init-addr none

backend authn
    option httpchk GET /services/authn/status/healthy
    http-check expect string STATUS_OK_
    server authn1 eduid-authn-1:8080 check resolvers docker_dns  init-addr none

backend security
    option httpchk GET /services/security/status/healthy
    http-check expect string STATUS_OK_
    server security eduid-security-1:8080 check resolvers docker_dns  init-addr none

backend pdata
    option httpchk GET /services/personal-data/status/healthy
    http-check expect string STATUS_OK_
    server pdata eduid-personal_data-1:8080 check resolvers docker_dns  init-addr none

backend email
    option httpchk GET /services/email/status/healthy
    http-check expect string STATUS_OK_
    server email eduid-email-1:8080 check resolvers docker_dns  init-addr none

backend freja-eid
    option httpchk GET /services/freja-eid/status/healthy
    http-check expect string STATUS_OK_
    server freja_eid1 eduid-freja_eid-1:8080 check resolvers docker_dns  init-addr none

backend favicon
    server html html.eduid.docker:443 ssl verify none  check resolvers docker_dns  init-addr none

frontend LB-http
    bind *:9000
    default_backend LB

backend LB
    stats enable
    #stats hide-version
    stats uri /haproxy_stats
